# ── Gateway (apps/gateway) — local dev harness ───────────────────────────────
# Host/port the mock gateway binds to when you run `npm run dev:gateway`.
HOST=127.0.0.1
PORT=8787
# Base URL the extension deep-links to for sign-in.
STUDIO_BASE_URL=https://luno.codes
